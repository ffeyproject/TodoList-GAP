export const metadata = {
  title: "Login - GAP TODOLIST APP",
};

export default function LoginLayout({ children }) {
  return (
    <html lang="en">
      <body className="login-body">{children}</body>
    </html>
  );
}
